# Codebase setup

Run once via `ucm transcript -S .ucm setup.md`. `run.sh` does this
for you.

``` ucm
scratch/main> project.create-empty unix
unix/main> lib.install @unison/base
unix/main> lib.install @unison/json
```
