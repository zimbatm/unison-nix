# Codebase setup

Run once via `ucm transcript -S .ucm setup.md`. `run.sh` does this
for you.

``` ucm
scratch/main> project.create-empty upkg
upkg/main> lib.install @unison/base
upkg/main> lib.install @unison/json
```
